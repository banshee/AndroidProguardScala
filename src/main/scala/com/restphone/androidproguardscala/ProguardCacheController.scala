package com.restphone.androidproguardscala

class ProguardCacheController {
  def build_proguard_dependency_files(conf: ProguardCacheParameters) = {
    
  }
  
  def run_proguard(conf: ProguardCacheParameters) = {
    
  }
  
  def install_proguard_output(conf: ProguardCacheParameters) = {
    
  }
  
  def clean_cache(dir: String) = {
    
  }
}

object ProguardCacheController {
  def buildCacheController(pluginDirectory: String) = new ProguardCacheController
}
